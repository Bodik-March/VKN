16 варіант 1 завдання
import math
x = float(input('Введіть Х: '))
y = (math.log(x,math.e)-math.log10(x)+4*x)/(math.fabs(math.cos(x)+0.4))+0.12*math.pow(3*x+3,1/5)
print(y)
16 варіант 2 завдання
import math
def func(x0,x1,x2):
    M = math.fabs(math.pow(math.sin(x0),2)-x1)+math.sqrt(math.fabs(x2)+2)-((x0+x1)/math.log(math.fabs(x2),8))
    return (M)
x=float(input('Ведіть X:'))
y=float(input('Ведіть Y:'))
z=float(input('Ведіть Z:'))
M=func(x,y,z)
print(M)
